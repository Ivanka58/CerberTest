export async function handleRules(ctx) {
  await ctx.reply(
    `📋 *Правила использования CerberAI*\n\n` +
    `1️⃣ *Уважение* — не оскорбляй бота и других пользователей\n` +
    `2️⃣ *Законность* — не используй для незаконной деятельности\n` +
    `3️⃣ *Конфиденциальность* — не передавай чужие личные данные\n` +
    `4️⃣ *Токены* — трать разумно, они конечны\n` +
    `5️⃣ *VIP* — если купил, не злоупотребляй\n\n` +
    `*Запрещено:*\n` +
    `❌ Генерация вредоносного контента\n` +
    `❌ Спам и флуд\n` +
    `❌ Попытки взлома\n\n` +
    `*Нарушение = бан* 🚫`,
    { parse_mode: "Markdown" }
  );
}
